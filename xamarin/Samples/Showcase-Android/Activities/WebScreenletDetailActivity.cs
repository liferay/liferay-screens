using Android.App;
using Android.OS;

namespace ShowcaseAndroid
{
    [Activity]
    public class WebScreenletDetailActivity : Activity
    {
        protected override void OnCreate(Bundle savedInstanceState)
        {
            base.OnCreate(savedInstanceState);
        }
    }
}
